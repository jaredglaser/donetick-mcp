{
  "name": "donetick-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test --isolate",
    "test:watch": "bun test --isolate --watch",
    "verify:live": "bun scripts/verify-live.ts",
    "verify:up": "bun scripts/verify-up.ts",
    "verify:down": "bun scripts/verify-down.ts"
  },
  "dependencies": {
    "@js-temporal/polyfill": "0.5.1",
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "6.0.3"
  }
}
